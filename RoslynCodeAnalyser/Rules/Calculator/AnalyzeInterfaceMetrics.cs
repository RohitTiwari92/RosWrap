using Core.Models;
using Rules.Model;

namespace Rules.Calculator
{
   public class AnalyzeInterfaceMetrics
    {
        public  void AnalyzeInterface(ProjectAnalysisData prj, ProjectMetrics metrics)
        {
            foreach (var inf in prj.Interfaces)
            {
                break;
            }
        }
    }
}
